<!DOCTYPE html>
<html lang="en" class="dark">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Solstice</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      darkMode: 'class',
    }
  </script>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>

<body class="bg-slate-900 text-slate-100 h-[100dvh]">

  <% if(typeof message !== 'undefined' && message.length > 0) { %>
  <div id="toast"
    class="fixed bottom-5 right-5 flex items-center w-full max-w-xs p-4 space-x-4 text-white bg-blue-600 rounded-lg shadow-lg opacity-0 transform translate-y-full duration-500"
    role="alert">
    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
        d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z">
      </path>
    </svg>
    <div class="font-medium"><%= message %></div>
  </div>
  <% } %>

  <!-- Navbar -->
  <nav class="bg-slate-800 border-b border-slate-700 shadow-sm sticky top-0 z-20">
    <div class="w-full px-4 sm:px-6 lg:px-8">
      <div class="flex justify-around items-center h-16">
        <h1 class="text-2xl font-bold mr-auto text-blue-500"><a href="/posts/homepage">Solstice</a></h1>
        <div class="flex items-center gap-4">
          <a href="/posts/addpost" title="Add Post" class="text-slate-300 hover:text-blue-400 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24"
              stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M12 9v3m0 0v3m0-3h3m-3 0H9m12 0a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
          </a>

          <a href="/user/myposts" title="My Posts" class="text-slate-300 hover:text-blue-400 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24"
              stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2H5a2 2 0 00-2 2v2m14 0h-2m-2 0h-2" />
            </svg>
          </a>

          <a href="/user/likedposts" title="Liked Posts" class="text-slate-300 hover:text-blue-400 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24"
              stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M4.318 6.318a4.5 4.5 0 016.364 0L12 7.636l1.318-1.318a4.5 4.5 0 116.364 6.364L12 20.364l-7.682-7.682a4.5 4.5 0 010-6.364z" />
            </svg>
          </a>

          <a href="/user/savedposts" title="Saved Posts" class="text-slate-300 hover:text-blue-400 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24"
              stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z" />
            </svg>
          </a>

          <a href="/user/logout" title="Logout" class="text-slate-300 hover:text-red-500 transition-colors">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24"
              stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round"
                d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1" />
            </svg>
          </a>

          <a href="/user/myprofile" title="My Profile">
            <img src="/images/uploads/<%= user.profilepic %>" alt="My Profile"
              class="w-10 h-10 rounded-full object-cover border-2 border-transparent hover:border-blue-500 transition-all" />
          </a>
        </div>
      </div>
    </div>
  </nav>

  <!-- Main -->
  <main class="max-w-2xl mx-auto py-8 px-4 sm:px-0">
    <% if(posts && posts.length > 0) { %>
      <% posts.slice().reverse().forEach(function(post) { %>
        <% if(post.image) { %>
        <div class="bg-slate-800 border border-slate-700 rounded-xl shadow-md mb-8">
          <div class="flex items-center p-4 justify-between">
            <a href="/user/profile/<%= post._id %>" class="flex items-center gap-3">
              <img src="/images/uploads/<%= post.userprofilepic %>" alt="#"
                class="w-10 h-10 rounded-full object-cover">
              <div>
                <p class="font-bold text-slate-100"><%= post.fullname %></p>
                <p class="text-xs text-slate-400"><%= post.time %></p>
              </div>
            </a>
            <% if(String(post.userid) === String(user._id)) { %>
              <a href="/posts/editpost/<%= post._id %>"
                class="text-sm font-medium text-slate-400 hover:text-blue-400">Edit</a>
            <% } %>
          </div>

          <a href="/posts/viewpost/<%= post._id %>">
            <img src="/images/uploads/<%= post.image %>" alt="<%= post.title %>" class="w-full h-auto">
          </a>

          <div class="p-4 flex items-center gap-4">
            <% const isLiked = post.likes.indexOf(user._id.toString()) !== -1; %>
            <a href="/posts/like/<%= post._id %>" class="flex items-center gap-2 text-slate-300 hover:text-red-500 transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7 <%= isLiked ? 'text-red-500' : '' %>"
                viewBox="0 0 20 20" fill="<%= isLiked ? 'currentColor' : 'none' %>" stroke="currentColor"
                stroke-width="1.5">
                <path fill-rule="evenodd"
                  d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z"
                  clip-rule="evenodd" />
              </svg>
            </a>
            <a href="#" class="flex items-center gap-2 text-slate-300 hover:text-blue-400 transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24"
                stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
              </svg>
            </a>
            <% const isSaved = post.saved.indexOf(user._id.toString()) !== -1; %>
            <a href="/posts/save/<%= post._id %>" title="Saved Posts"
              class="flex items-center gap-2 text-slate-300 hover:text-blue-400 transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg"
                class="h-6 w-6 transition-colors <%= isSaved ? 'text-blue-500' : '' %>" viewBox="0 0 24 24"
                fill="<%= isSaved ? 'currentColor' : 'none' %>" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z" />
              </svg>
            </a>
          </div>

          <div class="px-4 pb-4">
            <p class="font-semibold text-slate-300 text-sm"><%= post.likes.length %> <%= post.likes.length === 1 ? 'like' : 'likes' %></p>
            <p class="text-slate-200 mt-1">
              <a href="/user/profile/<%= post._id %>" class="font-bold text-slate-100 hover:underline">@<%= post.username %></a>
              <a href="/posts/viewpost/<%= post._id %>"><%= post.caption %></a>
            </p>
          </div>
        </div>

        <% } else { %>
        <div class="bg-slate-800 border border-slate-700 rounded-xl shadow-md mb-8 p-4">
          <div class="flex items-center justify-between mb-3">
            <a href="/user/profile/<%= post._id %>" class="flex items-center gap-3">
              <img src="/images/uploads/<%= post.userprofilepic %>" alt="<%= post.fullname %>"
                class="w-10 h-10 rounded-full object-cover">
              <div>
                <p class="font-bold text-slate-100"><%= post.fullname %></p>
                <p class="text-xs text-slate-400"><%= post.time %></p>
              </div>
            </a>
            <% if(String(post.userid) === String(user._id)) { %>
              <a href="/posts/editpost/<%= post._id %>"
                class="text-sm font-medium text-slate-400 hover:text-blue-400">Edit</a>
            <% } %>
          </div>
          <p class="text-slate-200 mb-3">
            <a href="/user/profile/<%= post._id %>" class="font-bold text-slate-100 hover:underline">@<%= post.username %></a>
            <a href="/posts/viewpost/<%= post._id %>"><%= post.caption %></a>
          </p>

          <div class="flex items-center gap-4">
            <% const isLiked = post.likes.map(id => id.toString()).includes(user._id.toString()); %>
            <a href="/posts/like/<%= post._id %>" class="flex items-center gap-2 text-slate-300 hover:text-red-500">
              <svg xmlns="http://www.w3.org/2000/svg"
                class="h-7 w-7 transition-colors <%= isLiked ? 'text-red-500' : '' %>" viewBox="0 0 20 20"
                fill="<%= isLiked ? 'currentColor' : 'none' %>" stroke="currentColor" stroke-width="1.5">
                <path fill-rule="evenodd"
                  d="M3.172 5.172a4 4 0 015.656 0L10 6.343l1.172-1.171a4 4 0 115.656 5.656L10 17.657l-6.828-6.829a4 4 0 010-5.656z"
                  clip-rule="evenodd" />
              </svg>
            </a>
            <a href="#" class="flex items-center gap-2 text-slate-300 hover:text-blue-400 transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7" fill="none" viewBox="0 0 24 24"
                stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
              </svg>
            </a>
            <% const isSaved2 = post.saved.indexOf(user._id.toString()) !== -1; %>
            <a href="/posts/save/<%= post._id %>" title="Saved Posts"
              class="flex items-center gap-2 text-slate-300 hover:text-blue-400 transition-colors">
              <svg xmlns="http://www.w3.org/2000/svg"
                class="h-6 w-6 transition-colors <%= isSaved2 ? 'text-blue-500' : '' %>" viewBox="0 0 24 24"
                fill="<%= isSaved2 ? 'currentColor' : 'none' %>" stroke="currentColor" stroke-width="1.5">
                <path stroke-linecap="round" stroke-linejoin="round"
                  d="M5 5a2 2 0 012-2h10a2 2 0 012 2v16l-7-3.5L5 21V5z" />
              </svg>
            </a>
          </div>

          <p class="font-semibold text-slate-300 text-sm mt-2">
            <%= post.likes.length %> <%= post.likes.length === 1 ? 'like' : 'likes' %>
          </p>
        </div>
        <% } %>
      <% }); %>
    <% } else { %>
      <div class="text-center bg-slate-800 border border-slate-700 p-12 rounded-xl shadow-md">
        <h2 class="text-2xl font-bold text-slate-100 mb-2">No Posts to Show</h2>
        <p class="text-slate-400">When you follow people, their posts will appear here.</p>
        <a href="/posts/addpost"
          class="mt-6 inline-block bg-blue-600 text-white px-6 py-2 rounded-lg font-semibold hover:bg-blue-700 transition-colors">
          Create the First Post
        </a>
      </div>
    <% } %>
  </main>

  <script>
    const toast = document.getElementById('toast');
    if (toast) {
      setTimeout(() => {
        toast.classList.remove('opacity-0', 'translate-y-full');
        toast.classList.add('opacity-100', 'translate-y-0');
      }, 100);
      setTimeout(() => {
        toast.classList.remove('opacity-100', 'translate-y-0');
        toast.classList.add('opacity-0', 'translate-y-full');
        setTimeout(() => toast.remove(), 500);
      }, 4000);
    }
</script>
</body>
</html>